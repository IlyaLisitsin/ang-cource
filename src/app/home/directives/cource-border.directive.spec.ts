import { CourceBorderDirective } from './cource-border.directive';

describe('CourceBorderDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new CourceBorderDirective();
    expect(directive).toBeTruthy();
  });
});
